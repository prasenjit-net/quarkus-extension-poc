plugins {
    id('java')
    id('io.quarkus')
}

description = 'Gradle POC Extension Integration Test'

dependencies {
    implementation platform("io.quarkus:quarkus-bom:${quarkusVersion}")
    implementation(project(':extension1'))
    implementation('io.quarkus:quarkus-resteasy')
}
